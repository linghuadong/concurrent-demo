<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.linghd.mapper.OrderLineMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_line (order_number, product_id, product_name, product_picture, product_price, num) VALUE (#{orderNumber},#{productId},#{productName},#{productPicture},#{productPrice},#{num});
    </insert>
</mapper>